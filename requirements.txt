torch>=2.0.0
torchvision
diffusers>=0.24.0
transformers
accelerate
pillow
scipy
safetensors
librosa
imageio
imageio-ffmpeg
huggingface-hub
ddgs
psutil

# ACE-Step 1.5 (Windows/Linux fallback voor muziekgeneratie)
# Wordt automatisch geinstalleerd als MLX niet beschikbaar is
# Handmatig: cd ACE-Step-1.5 && pip install -e .
diffusers>=0.28.0
soundfile

# MusicGen runtime deps (audiocraft zelf wordt --no-deps geinstalleerd)
flashy
encodec
sentencepiece

# Optioneel: lokale LLM (traag om te bouwen, niet nodig voor kerntaak)
# llama-cpp-python

